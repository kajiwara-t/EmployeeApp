<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kajiwaratakuya.employeeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmployeeMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".WorkPlaceOffice"
            android:label="@string/app_name"></activity>
        <activity android:name=".WorkPlaceTsukiji"
            android:label="@string/app_name"></activity>
        <activity android:name=".WorkPlaceWho"
            android:label="@string/app_name"></activity>
        <activity android:name=".EmployeeList"
            android:label="@string/app_name"></activity>

    </application>

</manifest>